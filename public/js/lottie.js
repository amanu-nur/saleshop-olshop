lottie.loadAnimation({
    container: document.getElementById('lottie-hero'), // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'Assets/Animations/hero.json' // the path to the animation json
  });

  lottie.loadAnimation({
    container: document.getElementById('lottie-success'), // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'Assets/Animations/success.json' // the path to the animation json
  });

